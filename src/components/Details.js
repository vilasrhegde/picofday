import { useState,useEffect } from 'react';
import React from 'react';

export const Details = ({ eventData }) => {
    const [image, setImage]=useState('')

     return <Details image={eventDat} />
    return (
        <div>
           <h1>{ image }</h1>
        </div>
    )
}
